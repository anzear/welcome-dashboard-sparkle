import { useEffect, useMemo, useState } from "react";
import { format } from "date-fns";
import { ArrowUpDown, Check, ExternalLink, FileText, History, Loader2, MoreHorizontal, Plus, ScrollText, Search, X } from "lucide-react";
import { toast } from "sonner";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Checkbox } from "@/components/ui/checkbox";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle } from "@/components/ui/sheet";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Textarea } from "@/components/ui/textarea";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip";
import { DerivedPathwayList, DerivedPathways, NodeChips, NodeFilterEmpty, RecordHistoryList, ReviewStatusChip, ScopeChip, TraceId, ValueCell, useHistorySheet, useNodeFilter } from "@/components/hitl";
import { derivedPathwayIds, useHitlStore, type EvidenceNodes, type EvidenceScope, type PaperPatentMatch, type Pathway, type ReviewStatus } from "@/lib/hitlStore";
import { evidenceNodeKeys, evidenceNodeLabels } from "./EvidenceMatchPrimitives";
import { lookupDoi, lookupPatent, type LookupKind, type LookupResult } from "@/lib/mockLookup";
import { cn } from "@/lib/utils";

type DecidedStatus = Exclude<ReviewStatus, "review_pending">;
type DecisionTarget = { ids: string[]; status: DecidedStatus } | null;
type SortKey = "default" | "scope" | "pathways";
const emptyNodes = (): EvidenceNodes => ({ feedstock: null, process_technology: null, product: null, application_market: null });
const normalize = (value: string | null) => value?.trim().toLocaleLowerCase() ?? "";
const externalUrl = (item: PaperPatentMatch) => item.kind === "paper" ? `https://doi.org/${item.external_id}` : `https://patents.google.com/patent/${item.external_id}`;
const nextId = (ids: string[]) => `pp-${String(Math.max(0, ...ids.map(id => Number(id.match(/\d+/)?.[0] ?? 0))) + 1).padStart(3, "0")}`;
const hasNode = (nodes: EvidenceNodes) => evidenceNodeKeys.some(key => Boolean(nodes[key]?.trim()));

function KindChip({ kind }: { kind: LookupKind }) { const Icon = kind === "paper" ? FileText : ScrollText; return <Badge variant="outline" className="h-5 gap-1 whitespace-nowrap text-[9px] font-normal text-muted-foreground"><Icon className="h-3 w-3" />{kind === "paper" ? "Paper" : "Patent"}</Badge>; }
function FilterSelect({ value, onChange, label, children }: { value: string; onChange: (value: string) => void; label: string; children: React.ReactNode }) { return <Select value={value} onValueChange={onChange}><SelectTrigger aria-label={label} className="h-8 w-40 text-[10px]"><SelectValue /></SelectTrigger><SelectContent>{children}</SelectContent></Select>; }
function Truncated({ value, className }: { value: string | null; className?: string }) { return value ? <Tooltip><TooltipTrigger asChild><span className={cn("block max-w-52 truncate text-[10px]", className)}>{value}</span></TooltipTrigger><TooltipContent className="max-w-md text-xs">{value}</TooltipContent></Tooltip> : <ValueCell value={null} />; }

function NodeFields({ nodes, onChange, pathways }: { nodes: EvidenceNodes; onChange: (nodes: EvidenceNodes) => void; pathways: Pathway[] }) {
  return <div className="grid gap-3 sm:grid-cols-2">{evidenceNodeKeys.map(key => { const options = [...new Set(pathways.map(pathway => pathway[key]))].sort(); const value = nodes[key] ?? ""; const isNew = Boolean(value.trim()) && !options.some(option => normalize(option) === normalize(value)); return <div key={key}><div className="mb-1 flex items-center justify-between"><Label htmlFor={`node-${key}`} className="text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">{evidenceNodeLabels[key]} (optional)</Label>{isNew && <Badge variant="outline" className="h-5 text-[9px] font-normal">New node</Badge>}</div><Input id={`node-${key}`} list={`options-${key}`} value={value} onChange={event => onChange({ ...nodes, [key]: event.target.value || null })} onBlur={() => { const canonical = options.find(option => normalize(option) === normalize(value)); if (canonical) onChange({ ...nodes, [key]: canonical }); }} className="h-9 text-xs" /><datalist id={`options-${key}`}>{options.slice(0, 8).map(option => <option key={option} value={option} />)}</datalist></div>; })}</div>;
}

function MatchEditor({ nodes, scope, note, onNodes, onScope, onNote, pathways }: { nodes: EvidenceNodes; scope: EvidenceScope; note: string; onNodes: (value: EvidenceNodes) => void; onScope: (value: EvidenceScope) => void; onNote: (value: string) => void; pathways: Pathway[] }) {
  return <div className="space-y-4"><NodeFields nodes={nodes} onChange={onNodes} pathways={pathways} /><div className="grid gap-3 sm:grid-cols-2"><div><Label className="text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Scope</Label><Select value={scope ?? "unclassified"} onValueChange={value => onScope(value === "unclassified" ? null : value as EvidenceScope)}><SelectTrigger className="mt-1 h-9 text-xs"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="production">Production</SelectItem><SelectItem value="application">Application</SelectItem><SelectItem value="unclassified">Unclassified</SelectItem></SelectContent></Select></div><div><Label className="text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Note (optional)</Label><Input className="mt-1 h-9 text-xs" value={note} onChange={event => onNote(event.target.value)} /></div></div>{!hasNode(nodes) && <p className="text-xs text-destructive">Match at least one node</p>}<div><h3 className="mb-2 text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Derived pathways</h3><DerivedPathwayList match={{ nodes }} /></div></div>;
}

export function MatchReviewSection({ kind }: { kind: LookupKind }) {
  const [decision, setDecision] = useState<DecisionTarget>(null); const [editId, setEditId] = useState<string | null>(null); const [detailId, setDetailId] = useState<string | null>(null); const [addOpen, setAddOpen] = useState(false);
  return <><MatchesTable kind={kind} onDecision={(ids, status) => setDecision({ ids, status })} onEdit={setEditId} onDetail={setDetailId} onAdd={() => setAddOpen(true)} /><DecisionDialog kind={kind} target={decision} onClose={() => setDecision(null)} /><EditNodesDialog kind={kind} matchId={editId} onClose={() => setEditId(null)} /><LookupDialog kind={kind} open={addOpen} onClose={() => setAddOpen(false)} onOpenExisting={setDetailId} /><MatchDetailSheet kind={kind} matchId={detailId} onClose={() => setDetailId(null)} onDecision={(ids, status) => setDecision({ ids, status })} onEdit={setEditId} /></>;
}

function MatchesTable({ kind, onDecision, onEdit, onDetail, onAdd }: { kind: LookupKind; onDecision: (ids: string[], status: DecidedStatus) => void; onEdit: (id: string) => void; onDetail: (id: string) => void; onAdd: () => void }) {
  const store = useHitlStore(); const { openHistory } = useHistorySheet(); const nodeFilter = useNodeFilter(); const entityType = kind === "paper" ? "paper_match" : "patent_match";
  const [search, setSearch] = useState(""); const [status, setStatus] = useState("all"); const [scope, setScope] = useState("all"); const [sort, setSort] = useState<SortKey>("default"); const [selected, setSelected] = useState<string[]>([]);
  const filtered = useMemo(() => store.paperPatentMatches.filter(item => { const ids = derivedPathwayIds(item, store.pathways); const haystack = [item.title, item.external_id, item.authors_or_assignee, item.note, ...evidenceNodeKeys.map(key => item.nodes[key])].join(" ").toLocaleLowerCase(); const directFilter = (!nodeFilter.feedstock || normalize(item.nodes.feedstock) === normalize(nodeFilter.feedstock)) && (!nodeFilter.product || normalize(item.nodes.product) === normalize(nodeFilter.product)); const derivedFilter = ids.some(id => nodeFilter.matchingPathwayIds.has(id)); return item.kind === kind && (!nodeFilter.isActive || directFilter || derivedFilter) && (!search || haystack.includes(search.toLocaleLowerCase())) && (status === "all" || item.status === status) && (scope === "all" || (scope === "unclassified" ? item.scope === null : item.scope === scope)); }).sort((a, b) => sort === "scope" ? String(a.scope ?? "unclassified").localeCompare(String(b.scope ?? "unclassified")) : sort === "pathways" ? derivedPathwayIds(b, store.pathways).length - derivedPathwayIds(a, store.pathways).length : (a.status === "review_pending" ? 0 : 1) - (b.status === "review_pending" ? 0 : 1) || +new Date(b.matched_at) - +new Date(a.matched_at)), [store.paperPatentMatches, store.pathways, kind, search, status, scope, sort, nodeFilter.feedstock, nodeFilter.product]);
  useEffect(() => setSelected([]), [nodeFilter.feedstock, nodeFilter.product]);
  const toggle = (id: string, checked: boolean) => setSelected(items => checked ? [...new Set([...items, id])] : items.filter(item => item !== id));
  return <><div className="flex flex-wrap items-center gap-2 border-b px-4 py-3"><div className="relative min-w-56 flex-1"><Search className="absolute left-2.5 top-2 h-3.5 w-3.5 text-muted-foreground" /><Input className="h-8 pl-8 text-xs" placeholder={`Search ${kind === "paper" ? "papers" : "patents"}…`} value={search} onChange={event => setSearch(event.target.value)} /></div><FilterSelect value={status} onChange={setStatus} label="Status"><SelectItem value="all">All statuses</SelectItem><SelectItem value="review_pending">Review pending</SelectItem><SelectItem value="accepted">Accepted</SelectItem><SelectItem value="rejected">Rejected</SelectItem></FilterSelect><FilterSelect value={scope} onChange={setScope} label="Scope"><SelectItem value="all">All scopes</SelectItem><SelectItem value="production">Production</SelectItem><SelectItem value="application">Application</SelectItem><SelectItem value="unclassified">Unclassified</SelectItem></FilterSelect><FilterSelect value={sort} onChange={value => setSort(value as SortKey)} label="Sort"><SelectItem value="default">Review priority</SelectItem><SelectItem value="scope">Scope</SelectItem><SelectItem value="pathways">Pathway count</SelectItem></FilterSelect><Button size="sm" className="ml-auto h-8 text-[10px]" onClick={onAdd}>{kind === "paper" ? <FileText className="mr-1 h-3 w-3" /> : <ScrollText className="mr-1 h-3 w-3" />}Add {kind} by {kind === "paper" ? "DOI" : "ID"}</Button></div>
  {selected.length > 0 && <div className="sticky top-0 z-10 flex items-center gap-2 border-b bg-background px-4 py-2 shadow-sm"><span className="text-xs font-medium">{selected.length} selected</span><Button variant="outline" size="sm" className="h-7 text-[10px] text-primary" onClick={() => onDecision(selected, "accepted")}><Check className="mr-1 h-3 w-3" />Accept</Button><Button variant="outline" size="sm" className="h-7 text-[10px] text-destructive" onClick={() => onDecision(selected, "rejected")}><X className="mr-1 h-3 w-3" />Reject</Button></div>}
  <div className="overflow-x-auto"><Table className="min-w-[1880px]"><TableHeader><TableRow><TableHead className="w-9"><Checkbox checked={filtered.length > 0 && filtered.every(item => selected.includes(item.id))} onCheckedChange={checked => setSelected(checked ? filtered.map(item => item.id) : [])} /></TableHead><TableHead>Title</TableHead><TableHead>{kind === "paper" ? "DOI" : "Patent ID"}</TableHead><TableHead>{kind === "paper" ? "Authors" : "Assignee"}</TableHead><TableHead>Year</TableHead><TableHead><Button variant="ghost" className="h-7 px-0 text-[10px]" onClick={() => setSort("scope")}>Scope<ArrowUpDown className="ml-1 h-3 w-3" /></Button></TableHead><TableHead>Nodes</TableHead><TableHead><Button variant="ghost" className="h-7 px-0 text-[10px]" onClick={() => setSort("pathways")}>Pathways<ArrowUpDown className="ml-1 h-3 w-3" /></Button></TableHead><TableHead>Matched</TableHead><TableHead>Status</TableHead><TableHead>Status changed</TableHead><TableHead>Note</TableHead><TableHead>Trace</TableHead><TableHead className="text-right">Actions</TableHead></TableRow></TableHeader><TableBody>{filtered.map(item => { const statusChanged = item.status_changed_at === item.matched_at ? null : item.status_changed_at; return <TableRow key={item.id}><TableCell><Checkbox checked={selected.includes(item.id)} onCheckedChange={checked => toggle(item.id, checked === true)} /></TableCell><TableCell><Button variant="link" className="h-auto max-w-60 justify-start p-0 text-left text-xs" onClick={() => onDetail(item.id)}><Truncated value={item.title} className="max-w-60" /></Button></TableCell><TableCell><a href={externalUrl(item)} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 font-mono text-[10px] text-primary hover:underline">{item.external_id}<ExternalLink className="h-2.5 w-2.5" /></a></TableCell><TableCell><Truncated value={item.authors_or_assignee} /></TableCell><TableCell><ValueCell value={item.year} /></TableCell><TableCell><ScopeChip scope={item.scope} /></TableCell><TableCell className="max-w-96"><NodeChips nodes={item.nodes} compact /></TableCell><TableCell><DerivedPathways match={item} /></TableCell><TableCell className="font-mono text-[10px]">{format(new Date(item.matched_at), "dd MMM yyyy")}</TableCell><TableCell><ReviewStatusChip status={item.status} /></TableCell><TableCell className="font-mono text-[10px]"><ValueCell value={statusChanged ? format(new Date(statusChanged), "dd MMM yyyy") : null} /></TableCell><TableCell><Truncated value={item.note} /></TableCell><TableCell><TraceId value={item.trace_id} /></TableCell><TableCell><div className="flex justify-end gap-1">{item.status !== "accepted" && <Button variant="ghost" size="icon" className="h-7 w-7 text-primary" aria-label={`Accept ${item.id}`} onClick={() => onDecision([item.id], "accepted")}><Check className="h-3.5 w-3.5" /></Button>}{item.status !== "rejected" && <Button variant="ghost" size="icon" className="h-7 w-7 text-destructive" aria-label={`Reject ${item.id}`} onClick={() => onDecision([item.id], "rejected")}><X className="h-3.5 w-3.5" /></Button>}<DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" size="icon" className="h-7 w-7" aria-label={`More actions for ${item.id}`}><MoreHorizontal className="h-3.5 w-3.5" /></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onClick={() => onEdit(item.id)}>Edit nodes</DropdownMenuItem><DropdownMenuItem onClick={() => openHistory(entityType, item.id)}><History className="mr-2 h-3.5 w-3.5" />History</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></TableCell></TableRow>; })}{filtered.length === 0 && <TableRow><TableCell colSpan={14} className="p-0">{nodeFilter.isActive ? <NodeFilterEmpty rows={`${kind} matches`} /> : <div className="flex h-32 items-center justify-center text-xs text-muted-foreground">No {kind} matches fit these filters.</div>}</TableCell></TableRow>}</TableBody></Table></div></>;
}

function DecisionDialog({ kind, target, onClose }: { kind: LookupKind; target: DecisionTarget; onClose: () => void }) { const store = useHitlStore(); const [note, setNote] = useState(""); useEffect(() => { if (target) setNote(""); }, [target]); const first = target?.ids.length === 1 ? store.paperPatentMatches.find(item => item.id === target.ids[0]) : null; const save = () => { if (!target) return; target.ids.forEach(id => { const item = store.paperPatentMatches.find(row => row.id === id); if (item && item.status !== target.status) store.recordChange({ entity_type: kind === "paper" ? "paper_match" : "patent_match", entity_id: id, field: "status", prior_value: item.status, new_value: target.status, operation: target.status === "accepted" ? "accept" : "reject", note: note.trim() || null }); }); toast.success("Decision recorded. Signal captured for matching."); onClose(); }; return <Dialog open={target !== null} onOpenChange={open => { if (!open) onClose(); }}><DialogContent><DialogHeader><DialogTitle>{target?.status === "accepted" ? "Accept evidence match" : "Reject evidence match"}</DialogTitle><DialogDescription>{first ? <span className="space-y-2"><span className="block">{first.title} · {first.external_id}</span><span className="flex flex-wrap gap-2"><ScopeChip scope={first.scope} /><NodeChips nodes={first.nodes} compact /><DerivedPathways match={first} /></span></span> : `${target?.ids.length ?? 0} matches`}</DialogDescription></DialogHeader>{target && <div className="space-y-4"><ReviewStatusChip status={target.status} /><div><Label>Note (optional)</Label><Textarea className="mt-1 text-xs" value={note} onChange={event => setNote(event.target.value)} /></div></div>}<DialogFooter><Button variant="outline" onClick={onClose}>Cancel</Button><Button onClick={save}>Confirm</Button></DialogFooter></DialogContent></Dialog>; }

function EditNodesDialog({ kind, matchId, onClose }: { kind: LookupKind; matchId: string | null; onClose: () => void }) { const store = useHitlStore(); const item = store.paperPatentMatches.find(row => row.id === matchId); const [nodes, setNodes] = useState<EvidenceNodes>(emptyNodes()); const [scope, setScope] = useState<EvidenceScope>(null); const [note, setNote] = useState(""); useEffect(() => { if (item) { setNodes({ ...item.nodes }); setScope(item.scope); setNote(""); } }, [matchId]); const changed = item && (evidenceNodeKeys.some(key => normalize(item.nodes[key]) !== normalize(nodes[key])) || item.scope !== scope); const save = () => { if (!item || !hasNode(nodes)) return; const entity_type = kind === "paper" ? "paper_match" : "patent_match"; evidenceNodeKeys.forEach(key => { const next = nodes[key]?.trim() || null; if (item.nodes[key] !== next) store.recordChange({ entity_type, entity_id: item.id, field: `nodes.${key}`, prior_value: item.nodes[key], new_value: next, operation: "update", note: note.trim() || null }); }); if (item.scope !== scope) store.recordChange({ entity_type, entity_id: item.id, field: "scope", prior_value: item.scope, new_value: scope, operation: "update", note: note.trim() || null }); const count = derivedPathwayIds({ nodes }, store.pathways).length; toast.success(`Nodes updated · ${count} derived ${count === 1 ? "pathway" : "pathways"}`); onClose(); }; return <Dialog open={matchId !== null} onOpenChange={open => { if (!open) onClose(); }}><DialogContent className="max-h-[92vh] overflow-y-auto sm:max-w-3xl"><DialogHeader><DialogTitle>Edit nodes</DialogTitle><DialogDescription>{item ? `${item.title} · ${item.external_id}` : "Update matched nodes and scope."}</DialogDescription></DialogHeader>{item && <MatchEditor nodes={nodes} scope={scope} note={note} onNodes={setNodes} onScope={setScope} onNote={setNote} pathways={store.pathways} />}<DialogFooter><Button variant="outline" onClick={onClose}>Cancel</Button><Button disabled={!item || !hasNode(nodes) || !changed} onClick={save}>Save changes</Button></DialogFooter></DialogContent></Dialog>; }

type FetchState = "idle" | "fetching" | "error" | "success";
function LookupDialog({ kind, open, onClose, onOpenExisting }: { kind: LookupKind; open: boolean; onClose: () => void; onOpenExisting: (id: string) => void }) { const store = useHitlStore(); const [externalId, setExternalId] = useState(""); const [state, setState] = useState<FetchState>("idle"); const [result, setResult] = useState<LookupResult | null>(null); const [error, setError] = useState(""); const [showMore, setShowMore] = useState(false); const [nodes, setNodes] = useState<EvidenceNodes>(emptyNodes()); const [scope, setScope] = useState<EvidenceScope>(null); const [note, setNote] = useState(""); useEffect(() => { if (!open) return; setExternalId(""); setState("idle"); setResult(null); setError(""); setShowMore(false); setNodes(emptyNodes()); setScope(null); setNote(""); }, [open, kind]); const duplicate = store.paperPatentMatches.find(item => item.external_id.toLocaleLowerCase() === externalId.trim().toLocaleLowerCase()); const fetchRecord = async () => { setState("fetching"); setError(""); setResult(null); try { setResult(kind === "paper" ? await lookupDoi(externalId) : await lookupPatent(externalId)); setState("success"); } catch (reason) { setError(reason instanceof Error ? reason.message : "Lookup failed"); setState("error"); } }; const save = () => { if (!result || !hasNode(nodes) || duplicate) return; const now = new Date().toISOString(); const id = nextId(store.paperPatentMatches.map(item => item.id)); const record: PaperPatentMatch = { id, kind, external_id: externalId.trim(), title: result.title, pathway_id: "", nodes: Object.fromEntries(evidenceNodeKeys.map(key => [key, nodes[key]?.trim() || null])) as unknown as EvidenceNodes, scope, status: "accepted", matched_at: now, note: note.trim() || null, year: result.year, authors_or_assignee: result.authors_or_assignee, abstract: result.abstract, source: result.source, created_at: now, updated_at: now, status_changed_at: now, last_actor: store.currentUser.name, trace_id: null }; store.recordChange({ entity_type: kind === "paper" ? "paper_match" : "patent_match", entity_id: id, field: null, prior_value: null, new_value: record, operation: "link_add", note: note.trim() || null }); const count = derivedPathwayIds(record, store.pathways).length; toast.success(`${kind === "paper" ? "Paper" : "Patent"} ${record.external_id} linked · ${count} derived ${count === 1 ? "pathway" : "pathways"}`); onClose(); }; return <Dialog open={open} onOpenChange={value => { if (!value) onClose(); }}><DialogContent className="max-h-[92vh] overflow-y-auto sm:max-w-3xl"><DialogHeader><DialogTitle>Add {kind === "paper" ? "paper by DOI" : "patent by ID"}</DialogTitle><DialogDescription>Fetch a local mock record, then match it to one or more spine nodes.</DialogDescription></DialogHeader><div className="space-y-5"><div><Label>{kind === "paper" ? "DOI" : "Patent ID"}</Label><div className="mt-1 flex gap-2"><Input disabled={state === "fetching"} value={externalId} onChange={event => { setExternalId(event.target.value); if (state !== "fetching") setState("idle"); }} /><Button disabled={!externalId.trim() || state === "fetching" || Boolean(duplicate)} onClick={() => void fetchRecord()}>{state === "fetching" && <Loader2 className="mr-1 h-4 w-4 animate-spin" />}{state === "fetching" ? "Fetching" : "Fetch"}</Button></div>{duplicate && <div className="mt-2 flex items-center justify-between rounded-md border border-destructive/30 bg-destructive/5 p-2 text-xs text-destructive"><span>Already exists: {duplicate.id}</span><Button variant="link" className="h-auto p-0 text-xs" onClick={() => { onClose(); onOpenExisting(duplicate.id); }}>Open</Button></div>}{state === "error" && <p className="mt-2 text-xs text-destructive">{error}</p>}</div>{state === "success" && result && <><div className="space-y-2 rounded-md border p-4"><div className="flex items-center gap-2"><KindChip kind={kind} /><Badge variant="outline" className="text-[9px] font-normal">{result.source}</Badge><span className="text-[10px] text-muted-foreground">{result.year}</span></div><h3 className="text-sm font-semibold">{result.title}</h3><p className="text-xs text-muted-foreground">{kind === "paper" ? "Authors" : "Assignee"}: {result.authors_or_assignee}</p><p className={cn("text-xs leading-relaxed text-muted-foreground", !showMore && "line-clamp-3")}>{result.abstract}</p><Button variant="link" className="h-auto p-0 text-[10px]" onClick={() => setShowMore(value => !value)}>{showMore ? "Show less" : "Show more"}</Button></div><MatchEditor nodes={nodes} scope={scope} note={note} onNodes={setNodes} onScope={setScope} onNote={setNote} pathways={store.pathways} /></>}</div><DialogFooter><Button variant="outline" onClick={onClose}>Cancel</Button><Button disabled={state !== "success" || !result || !hasNode(nodes) || Boolean(duplicate)} onClick={save}><Plus className="mr-1 h-4 w-4" />Link {kind}</Button></DialogFooter></DialogContent></Dialog>; }

function MatchDetailSheet({ kind, matchId, onClose, onDecision, onEdit }: { kind: LookupKind; matchId: string | null; onClose: () => void; onDecision: (ids: string[], status: DecidedStatus) => void; onEdit: (id: string) => void }) { const store = useHitlStore(); const item = store.paperPatentMatches.find(row => row.id === matchId); if (!item) return <Sheet open={false}><SheetContent /></Sheet>; return <Sheet open={matchId !== null} onOpenChange={open => { if (!open) onClose(); }}><SheetContent className="flex w-[min(96vw,780px)] flex-col gap-0 p-0 sm:max-w-[780px]"><SheetHeader className="border-b px-5 py-4 pr-12"><div className="mb-1 flex flex-wrap items-center gap-2"><KindChip kind={kind} /><Badge variant="outline" className="text-[9px] font-normal">{item.source ?? "Unknown source"}</Badge><ScopeChip scope={item.scope} /><ReviewStatusChip status={item.status} /></div><SheetTitle className="text-left text-base leading-snug">{item.title}</SheetTitle><SheetDescription className="text-left"><a href={externalUrl(item)} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 font-mono text-[10px] text-primary hover:underline">{item.external_id}<ExternalLink className="h-3 w-3" /></a></SheetDescription></SheetHeader><div className="min-h-0 flex-1 space-y-5 overflow-y-auto px-5 py-4"><section><div className="mb-2 flex items-center justify-between"><h3 className="text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Matched nodes</h3><Button variant="outline" size="sm" className="h-7 text-[10px]" onClick={() => onEdit(item.id)}>Edit nodes</Button></div><div className="grid gap-2 rounded-md border p-3 sm:grid-cols-4">{evidenceNodeKeys.map(key => <div key={key}><p className="text-[9px] text-muted-foreground">{evidenceNodeLabels[key]}</p>{item.nodes[key] ? <p className="mt-1 text-[10px] font-bold">{item.nodes[key]}</p> : <p className="mt-1 text-[10px] text-muted-foreground">not matched</p>}</div>)}</div></section><section><h3 className="mb-2 text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Derived pathways</h3><DerivedPathwayList match={item} /></section><section><h3 className="mb-2 text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Record</h3><div className="grid gap-3 rounded-md border p-3 text-xs sm:grid-cols-2"><div><span className="text-muted-foreground">Year</span><p><ValueCell value={item.year} /></p></div><div><span className="text-muted-foreground">{item.kind === "paper" ? "Authors" : "Assignee"}</span><p><ValueCell value={item.authors_or_assignee} /></p></div><div className="sm:col-span-2"><span className="text-muted-foreground">Abstract</span><p className="mt-1 leading-relaxed"><ValueCell value={item.abstract} /></p></div></div></section><section className="grid gap-3 sm:grid-cols-2"><div><h3 className="mb-2 text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Dates</h3><div className="space-y-1 font-mono text-[10px]"><p>Matched · {format(new Date(item.matched_at), "dd MMM yyyy, HH:mm:ss")}</p><p>Status changed · {format(new Date(item.status_changed_at), "dd MMM yyyy, HH:mm:ss")}</p></div></div><div><h3 className="mb-2 text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">Note and trace</h3><p className="mb-1 text-xs"><ValueCell value={item.note} /></p><TraceId value={item.trace_id} /></div></section><div className="flex flex-wrap gap-2 border-y py-3">{item.status !== "accepted" && <Button size="sm" className="h-8 text-[10px]" onClick={() => onDecision([item.id], "accepted")}><Check className="mr-1 h-3 w-3" />Accept</Button>}{item.status !== "rejected" && <Button variant="outline" size="sm" className="h-8 text-[10px] text-destructive" onClick={() => onDecision([item.id], "rejected")}><X className="mr-1 h-3 w-3" />Reject</Button>}<Button variant="outline" size="sm" className="h-8 text-[10px]" onClick={() => onEdit(item.id)}>Edit nodes</Button></div><section><h3 className="mb-2 text-[10px] font-semibold uppercase tracking-widest text-muted-foreground">History</h3><RecordHistoryList entityType={kind === "paper" ? "paper_match" : "patent_match"} entityId={item.id} /></section></div></SheetContent></Sheet>; }